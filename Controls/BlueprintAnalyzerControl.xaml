<UserControl x:Class="SpaceEditor.Controls.BlueprintAnalyzerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Panel -->
        <Border Grid.Row="0" Background="#1E1E1E" CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="BtnSelectBlueprint" Content="Analyze .vrb Blueprint" 
                        Width="180" Height="35" FontWeight="Bold" 
                        Click="BtnSelectBlueprint_Click"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Blueprint: " Foreground="Gray" FontSize="14"/>
                    <TextBlock x:Name="TxtBlueprintName" Text="None Selected" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,20,0"/>

                    <TextBlock Text="Total Blocks: " Foreground="Gray" FontSize="14"/>
                    <TextBlock x:Name="TxtTotalBlocks" Text="0" Foreground="White" FontSize="14" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading / Status Indicator -->
        <TextBlock x:Name="TxtStatus" Grid.Row="1" Text="" Foreground="#00FF00" 
                   FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10" Visibility="Collapsed"/>

        <!-- Component DataGrid -->
        <DataGrid x:Name="DgComponents" Grid.Row="2" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column"
                  AlternatingRowBackground="#2A2A2A" Background="#121212" RowBackground="#1E1E1E" 
                  Foreground="White" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#333333">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Component Name" Binding="{Binding ComponentName}" Width="*"/>
                <DataGridTextColumn Header="Required Amount" Binding="{Binding Amount}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>